import type { MonthlyDisciplineStats } from "../../utils/disciplineMonthly";
type Props = { data: MonthlyDisciplineStats };
export default function MonthlyDisciplineCard({ data }: Props) { const { stats } = data; return <article className="discipline-month-card"><div className="discipline-month-card__heading"><div><h3>{data.monthLabel}</h3><p>{stats.completed} réalisées sur {stats.planned}</p></div><strong>{stats.percentage}%</strong></div><div className="discipline-month-card__bar"><span style={{ width: `${stats.percentage}%` }} /></div><div className="discipline-month-card__metrics"><span><b>{stats.planned}</b> prévues</span><span><b>{stats.pending}</b> à venir</span><span><b>{stats.missed}</b> manquées</span></div></article>; }
